# TODO: add templates
- CI pipeline (PR)
    - validate new version tag
    - validate hydration (hydrate.sh)
    - validate yaml (kubeval, nomos vet)
    - validate policies (gatekeeper)
    - Integration test, when applicable
    - Upgrade test, when applicable
- CD pipeline (merge to main)
    - tag repo